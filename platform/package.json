{
  "name": "mha-m2-spike",
  "version": "0.1.0",
  "private": true,
  "description": "MyHotelAuditor.com — M2 website-analysis spike (docs/07 §4). Ratifies or kills Option A.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "node scripts/migrate.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "@electric-sql/pglite": "^0.2.17",
    "@types/node": "^26.1.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "postgres": "^3.4.5",
    "zod": "^3.24.1"
  }
}
