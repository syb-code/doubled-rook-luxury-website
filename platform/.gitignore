# Secrets — never committed (CLAUDE.md, working style)
.dev.vars
.dev.vars.*
!.dev.vars.example

node_modules/
.wrangler/
dist/
*.log

# Local spike artifacts (R2 stand-in, captured payloads)
.artifacts/
